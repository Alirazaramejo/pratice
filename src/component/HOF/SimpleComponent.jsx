// SimpleComponent.js (Regular Component)
import React from 'react';

const SimpleComponent = ({ greeting }) => {
  return (
    <div>
      <p>{greeting}</p>
    </div>
  );
};

export default SimpleComponent;
